[package]
name = "gbrs-core"
version = "0.2.0"
edition = "2018"

[dependencies]
lazy_static = { version =  "1.4.0", optional = true }
smallvec = "1.8.0"

[features]
default = ["std", "sound"]
std = ["lazy_static"]
sound = []
